{
  "name": "solar-system-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Real-time solar system simulator using official NASA/JPL ephemerides",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "pnpm --filter @sss/web dev",
    "build": "pnpm --filter @sss/web build",
    "preview": "pnpm --filter @sss/web preview",
    "fetch:data": "pnpm --filter @sss/tools fetch:data",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
